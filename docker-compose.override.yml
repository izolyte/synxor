# Dev overrides — exposes postgres, minio, and redis to localhost for local tooling
# Merged automatically by `docker compose up` (no extra flags needed)
services:
  postgres:
    ports:
      - "5432:5432"

  minio:
    ports:
      - "9000:9000"

  redis:
    ports:
      - "6379:6379"
